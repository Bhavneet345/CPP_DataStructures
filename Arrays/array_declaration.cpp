#include<iostream>
using namespace std;

//different array initialization

int main()
{
    int arr[10] = {0}; //declares an array of size 10 with all zeros.
    int arr1[10] = {1, 2, 3, 4}; //places as per specification.
    int arr[100] = {100}; //places 100 at only 1st position.
}